<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#MainMenu">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ path('dahsboard') }}">{{ brand }}</a>
    </div>

    <div class="collapse navbar-collapse" id="MainMenu">
      <ul class="nav navbar-nav">
        <li class="{{ route == 'dahsboard' ? 'active' }}">
          <a href="{{ path('dahsboard') }}">Dashboard</a>
        </li>
        <li class="{{ route starts with 'post' and not isPage ? 'active' }}">
          <a href="{{ path('posts') }}">Articles</a>
        </li>
        <li class="{{ route starts with 'post' and isPage ? 'active' }}">
          <a href="{{ path('posts', { isPage: true }) }}">Pages</a>
        </li>
        <li class="{{ route == 'users' ? 'active' }}">
          <a href="{{ path('users') }}">Users</a>
        </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li><a href="{{ path('fos_user_security_logout') }}">Sign out <i class="fa fa-fw fa-sign-out"></i></a></li>
      </ul>
    </div>{# /.navbar-collapse #}
  </div>{# /.container-fluid #}
</nav>
